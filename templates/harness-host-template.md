# Harness 机器配置 · 宿主参数（<host>）

> 生成说明：本文件是**单宿主异构参数**的住所——文件名=<host>.config.md（zcode/claude/kimi/codex/opencode/pi…），
> 每宿主一份，仅本宿主的项目消费；新宿主接入时装安装助手按问答生成（common 不动），已有则只读沿用。
> **安装结束后只能人工修订**（红线；agent 只备料）。单宿主差异改本文件、单项目差异用项目级
> harness.config.md 覆盖、确属全机变更才改 common——影响面多大就在哪层改。
> 解析约定同 common（表头识别、说明列全角｜、schema 缺失=fail-loud）。

| 键 | 值 | 说明 |
|---|---|---|
| schema | 1 | 配置版本 |
| agent_segment | 10-ZCODE | 本宿主 agent 段完整目录名（="10-"+宿主名大写；路径拼接零逻辑）。宿主特有覆盖项未来落此文件 |

## 修订记录（只追加，不改写）

| 日期 | 变更 | 动机 |
|---|---|---|
| （安装日期） | init <host/日期> | 首装生成（agent 生成 + 人复核 = 人授权） |
