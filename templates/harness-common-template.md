# Harness 机器配置 · 共享参数（common）

> 生成说明（安装 S5 生成后本段保留、值按问答替换）：本文件是**全机共享参数**的唯一住所——
> 所有 AI agent、所有项目共用一份；由安装助手在首装机器时按 S1/S5 问答生成（人复核=人授权），
> 生成与修订记录见文末修订记录表。**安装结束后只能人工修订**（红线；agent 任何场景只备料
> 给建议 diff，人执行）。格式=Markdown 表格三列（键|值|说明）；解析约定：以表头行
> `| 键 | 值 | 说明 |` 识别表格、仅解析表格内行，说明列禁用半角 `|`（用全角｜），
> schema 键缺失=配置非法（fail-loud）。牵动 vault 目录的键（zone_*）改动须联动数据迁移，
> 非纯文本替换。模板示例值即通用默认（spec 19 r9：全局单一口径）。

| 键 | 值 | 说明 |
|---|---|---|
| schema | 1 | 配置版本，供未来迁移 |
| vault_root | ~/Documents/MyObsidian | Obsidian 库根（S1 问答落此；展开为本机绝对路径） |
| zone_final_project | 50-项目库 | 项目定稿区（只能人写） |
| zone_final_common | 60-知识库 | 通用定稿区（只能人写） |
| zone_workspace | 70-工作区 | 工作区（agent 可写；folder 标签以此前缀=未整理信号） |
| harness_repo | ~/.agents/Harness-Engineering | 规范仓根（docs/ 在其下；非标准落位机写实际 clone 路径） |

## 修订记录（只追加，不改写）

| 日期 | 变更 | 动机 |
|---|---|---|
| （安装日期） | init <host/日期> | 首装生成（agent 生成 + 人复核 = 人授权） |
